{
  "name": "comment",
  "github": {
    "silent": true
  },
  "builds": [
    {
      "src": "index.cjs",
      "use": "@vercel/node",
      "config": {
        "includeFiles": [
          "../packages/server/**"
        ],
        "installCommand": "cd .. && npm install --no-frozen-lockfile && cd example",
        "buildCommand": "echo 'Skipping build step'"
      }
    }
  ],
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "index.cjs"
    }
  ]
}