{
  "name": "comment",
  "github": {
    "silent": true
  },
  "builds": [
    {
      "src": "index.cjs",
      "use": "@vercel/node",
      "config": {
        "includeFiles": [
          "../server/**"
        ],
        "installCommand": "cd ../server && npm install && cd ../example",
        "buildCommand": "echo 'Skipping build step'"
      }
    }
  ],
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "index.cjs"
    }
  ]
}